{
  "name": "nvim-lua-config",
  "version": "1.0.0",
  "description": "Here is the Work in Progress to configure NeoVim as the perfect IDE for WordPress development",
  "scripts": {
    "lint": "stylua --check lua && markdownlint README.md",
    "format": "stylua lua && markdownlint -f README.md",
    "setup": "nvim +PackerSync",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "markdownlint-cli": "^0.31.1",
    "husky": "^8.0.0"
  }
}
